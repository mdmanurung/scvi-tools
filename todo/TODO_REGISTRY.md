# TODO Registry

Tracks future work items, ideas, and planned improvements.

**Reconciled 2026-08-06** against `.claude/last-session.md`, `benchmarks/ANALYSIS_MANIFEST.md`,
`.living/`, the untracked `docs/plans/` + `docs/review-clear-execute/` packets,
`.scratch/mrtotalvi-rdx03-latent-integrity-v2/`, and 111 Codex session rollouts
(2026-06-26 → 2026-07-31). This table is the single source of truth for open work; a one-page
orientation brief is at [2026-08-06-remaining-work.md](2026-08-06-remaining-work.md), and the
phased sequencing plan — what to do in what order, and which gates are HITL — is at
[2026-08-06-next-steps-plan.md](2026-08-06-next-steps-plan.md).

**Tracks** — `mrtotalvi` (MrTotalVI/MrMultiVI, active) · `cytoanvi` (shelved 2026-07-12) ·
`infra` (shared).

**Status vocabulary** — `open` (actionable now) · `ready-for-human` (actionable now, but only you
can do it — a decision or an authorization; the canonical triage label from `AGENTS.md`) ·
`in_progress` · `blocked` (waiting on something outside this row, named in the title) · `shelved`
(deliberately parked; revival condition named in the title) · `deferred` · `done`.

| Title | Track | Priority | Status | Category | Date | Author | File |
|-------|-------|----------|--------|----------|------|--------|------|
| RDX-03-08a: RESOLVED 2026-08-06 — **the preflight is green; nothing needed rebuilding.** `pre_submit.py --read-only` first failed with `snapshot runtime identity drifted` (exit 2) when run from `res-hpc-gpu14`. Diagnosis: `runner_code_digest` and `runner_manifest_digest` matched exactly, conda_meta was 449/449 unchanged with zero new packages, and the sealed env-var dict was identical; the sole difference was `native_runtime` (file_count 778 → 779, total_bytes +96,284,520) — a byte-exact match for `/usr/lib64/libcuda.so.580.159.04`, which `ldd` resolves on GPU nodes only. Re-ran on CPU node `res-hpc-exe043` (job 25374994, `libcuda present: NONE`): **`ready: true`, exit 0**, `head=cb343ca1…`, `complete_dirty_status_sha256=fe4dcf97…`, `active.queue_rows=[]`, `claim_available=true`, `factual_human_da=locked_not_computed_or_inspected`, `grid_size=48`. So snapshot `f1b51341…` is VALID, and the working tree — including this reconciliation's `todo/` files — passes the repository gate as-is. There was never a manual re-baseline step: the dirty-status digest is computed dynamically at run time. LESSON: always run the preflight on a CPU node; the contract is `cpu_unless_separately_authorized` | mrtotalvi | critical | done | infrastructure | 2026-08-06 | mdmanurung | — |
| RDX-03-08b: run the bounded mixed/B1/seed-0 probe (Issue 08, `ready-for-agent`) — **THE NEXT ACTION.** Never run: `evidence/` holds dirs for issues 01–07 and none for 08, every acceptance box unchecked. Preconditions are now all met — Issue 07 APPROVED with all high/medium findings closed, snapshot `f1b51341…` verified valid, and the CPU preflight green (RDX-03-08a). Exact command, verbatim from `docs/review-clear-execute/mrtotalvi-rdx03-latent-integrity-v2/plan.md` §4.3: `python -m benchmarks.mrtotalvi.run_convergence_diagnosis --fixtures mixed --rows B1 --seeds 0`. Subset execution auto-records `run_purpose=probe` / `authoritative_full_grid=false` / `d0_decision=not_available_for_non_authoritative_grid` (`run_convergence_diagnosis.py:880,1676`), so it cannot be mistaken for the 48-fit aggregate. Must run CPU-only. A ready-to-submit batch script is at `/exports/para-lipg-hpc/mdmanurung/rdx03-preflight-diag/probe.sbatch` (partition `all`, 8 CPU, 64 G, 12 h, output outside the repo) — **submission was blocked by this session's permission classifier, so it needs you to run `sbatch` or grant the permission**. AMENDED 2026-08-06 (later, after BATCH-EMBEDDING landed): the precondition sentence above is now only partly true. The CPU preflight *result* from job 25374994 is stale, because `git status` over the live tree changed when the three mrtotalvi files were edited. The sealed snapshot itself is **not** stale — verified by `diff -rq` against `source-snapshots/f1b51341…`: exactly 3 files differ (`_components.py`, `_model.py`, `_module.py`, all from BATCH-EMBEDDING) and `benchmarks/` is byte-identical. And the grid never reads the live tree: `relaunch-rdx03-grid.sbatch:80,93,96` sets `PYTHONPATH=$SNAPSHOT_ROOT/src`, `GIT_WORK_TREE=$SNAPSHOT_ROOT` and `cd $SNAPSHOT_ROOT`, so the run executes entirely from the frozen snapshot and the batch-embedding change is simply absent from it (correct — it is out of RDX-03's scope). Reading `_validate_runtime` (`pre_submit.py:2033-2053`) confirms the only live-repo binding is `expected_head`, which is unchanged — so the preflight should still return `ready: true`. RE-RUN IT ON A CPU NODE FIRST anyway to get a current result, since the authorization payload is derived from a passing read-only run | mrtotalvi | critical | open | validation | 2026-08-06 | mdmanurung | — |
| RDX-03-09: **HITL** — grant one-use scheduler authorization for exactly one `sbatch` (Issue 09, `ready-for-human`). All acceptance boxes unchecked, Comments empty. CORRECTED 2026-08-06: `launch/authorizations/` does **not** exist at all — earlier notes said it "holds only the placeholder scheme". The contract names it as `authorization_root` and `pre_submit.py` builds the authorization payload from a passing read-only result (`pre_submit.py:2276`), so the directory is created on the authorized path rather than pre-scaffolded; nothing is lost, but there is no placeholder file to inspect beforehand. Issue 07 records "`sbatch` calls: zero". This single decision blocks the entire remaining MrTotalVI programme. Needs nothing external — only you | mrtotalvi | critical | ready-for-human | analysis | 2026-08-06 | mdmanurung | — |
| RDX-03-10: submit and monitor the single 48-fit RDX-03 grid (~44 h, 4 fixtures × B1/B2/B3/D0 × seeds 0/1/2). Go through `launch/pre_submit.py`, NOT `relaunch-rdx03-grid.sbatch` directly — that is the stale pre-ADR-0009 pathway that the 2026-08-03 run used and that fail-closed. No retry, no splice, no second submission | mrtotalvi | critical | blocked | analysis | 2026-08-06 | mdmanurung | — |
| RDX-03-11: seal the grid and independently recompute RDX-03 under policy `mrtotalvi-rdx03-latent-integrity-v2` (Issue 11) | mrtotalvi | critical | blocked | validation | 2026-08-06 | mdmanurung | — |
| RDX-03-12: **HITL** — review the RDX-03 decision and hand valid evidence to RDX-04, distinguishing a valid negative result from execution blockage. Per ADR-0009, RDX-03 itself cannot issue `candidate` or terminal `stop` | mrtotalvi | high | blocked | analysis | 2026-08-06 | mdmanurung | — |
| RDX-04: D1–D5 redesign screen — NEVER STARTED. Stage A = B1/B2/B3/D0 + D1–D5 at seed 0, ≤2 survivors; Stage B = survivors + B1/B2 at 3 seeds. Codex 2026-07-31: "D1–D5 implementation remains a separate feature after issue 12" | mrtotalvi | high | blocked | analysis | 2026-08-06 | mdmanurung | [../docs/plans/2026-07-26-mrtotalvi-stable-latent-da-redesign.md](../docs/plans/2026-07-26-mrtotalvi-stable-latent-da-redesign.md) |
| RDX-05…RDX-10: screen → public API → Milo DA → human non-inferiority → selection → final evidence report. All `[ ]` unchecked in `docs/review-clear-execute/mrtotalvi-v2-redesign/tasks.md` | mrtotalvi | high | blocked | analysis | 2026-08-06 | mdmanurung | — |
| MRTL-09/10/11 — **ID MAPPING CORRECTED 2026-08-06; this row previously double-counted work already in the RDX packet.** The Milo bridge is **MRTL-07 ≡ RDX-07**, not MRTL-09; MRTL-08 ≡ RDX-05 + RDX-09; MRTL-11 ≡ RDX-06 + RDX-10. All of those are tracked as RDX rows and phases, not here. What remains genuinely extra under the MRTL ids is: **MRTL-09** (LEMUR / miloDE / pseudobulk DE comparators — which `MRTL-10` itself calls optional, and which **becomes moot if T0.1 drops DE from the v1 claim**) and **MRTL-10** (publication-scale simulation + untouched macaque confirmation — overlaps `P2-005`, data-blocked). The legacy plan file still reads `Status: not started` and is superseded by `docs/review-clear-execute/mrtotalvi-v2-redesign/tasks.md`. Context that still holds: `benchmarks/mrtotalvi/README.md` states plainly "The current runner scores latent and decoder recovery. It does not implement Milo inference, calibrated DA FDR/power, LEMUR, miloDE, or pseudobulk DE" | mrtotalvi | medium | blocked | analysis | 2026-08-06 | mdmanurung | [2026-08-06-next-steps-plan.md](2026-08-06-next-steps-plan.md) |
| V2-LAND: triage and commit the uncommitted v2 body — 778 lines of `src/` diff, untracked `_counterfactual.py` (75 KB), `_seed.py`, `benchmarks/mrtotalvi/` (30 modules), `tests/benchmarks/mrtotalvi/` (23 files), ADRs 0007/0008/0009, 2 plan trackers, `docs/review-clear-execute/`, `src/scvi/_skills/`. RISK: `src/scvi/external/__init__.py` imports `combine_mrtotalvi_seed_results` from the untracked `_seed.py`, so `git clean -fdx` breaks the package import AND destroys the work. Codex sealed it 2026-07-25 as package-ready, 964 CPU tests passing, deliberately not promoted. TIMING — HARDENED 2026-08-06: the launch contract pins `expected_head: cb343ca1628b83f78b256b85e6cf218fb92dff3f`, and the CPU preflight (job 25374994) now passes `ready: true` against exactly that HEAD. **Committing anything would move HEAD and break the green gate**, forcing a contract refresh before the grid can run. So do NOT commit until RDX-03-11 seals — or, if you want it landed first, commit now and accept that the contract must be re-rendered and re-reviewed. This is a sequencing decision, not a technical blocker | mrtotalvi | high | ready-for-human | infrastructure | 2026-08-06 | mdmanurung | — |
| T0.1 DECISION: decide whether `store_lfc` DE stays in the v1 claim. Anti-concordant with the pseudobulk gold standard across all 12 cell types tested (Spearman −0.24 to +0.04, F-037); `benchmarks/ANALYSIS_MANIFEST.md` already rates it "not ready for bio conclusions / directional sanity check only". EVIDENCE ASSEMBLED 2026-08-06 (full box in the brief): F-037 gives MrTotalVI Spearman ρ **−0.126 to −0.008 — every one of the 12 cell types negative**; IFN gene × cell-type direction called correctly 19.9% (MrTotalVI), 17.9% (MRVI), 42.9% (MrMultiVI), all below the 50% of guessing; no rescue cell type (Classical monocytes, the strongest pseudobulk IFN signal, give 0/7 correct); and F-036 shows the **reference MRVI implementation is itself anti-concordant (ρ=−0.138)**, so the limitation is inherited from eps-space DE as a method rather than introduced here. Ground truth (F-029): pseudobulk has IFN UP at W22 while the models said down; F-020's narrative is retracted. RECOMMENDATION: drop DE from the v1 claim, keep `store_lfc` as a documented API with its disclaimer. Needs nothing external — only your decision | mrtotalvi | high | ready-for-human | analysis | 2026-08-06 | mdmanurung | — |
| T0.2: DA reliability is validated on one confounded cohort only — the schisto DTP cohort has NMI(donor,batch)=0.796, so donor-specific integration cannot be tested on it at all. Same unblock condition as P2-005 | mrtotalvi | high | blocked | analysis | 2026-08-06 | mdmanurung | — |
| PREFLIGHT-HOST-DOC: `launch/README.md` documents the `--read-only` preflight invocation but never says which host class to run it on. Run it on a GPU node and it fails `snapshot runtime identity drifted` for a reason that looks alarming and is not real — `ldd` resolves `/usr/lib64/libcuda.so.580.159.04` there, adding one file and 96,284,520 bytes to the native-runtime set. Cost 2026-08-06: several hours of diagnosis. Add one line to that README stating the preflight must run on a CPU node, consistent with `accelerator_policy=cpu_unless_separately_authorized` | mrtotalvi | low | open | documentation | 2026-08-06 | mdmanurung | — |
| BATCH-EMBEDDING: DONE 2026-08-06 — `MrTotalVI` accepts `batch_representation="embedding"` with full encoder+decoder parity, contained to `src/scvi/external/mrtotalvi/`. Shared helpers `_covariate_n_input`/`_append_covariates` gained optional `batch_dim`/`batch_rep`; the u-encoder takes the embedding; the decoder is rebuilt without the batch category, widened by `embedding_dim`, and wrapped in a new `BatchEmbeddingDecoderAdapter` so TOTALVAE's single `generative` call site is unchanged. Per-batch parameter tables (dispersion, `log_per_batch_efficiency`, protein background prior, library priors) stay one-hot indexed, matching `VAE`. `"one-hot"` remains the default and is numerically unchanged, pinned by a seeded latent-equality guard. 7 tests added, all passing; full mrtotalvi+mrmultivi suites 125 passed; `ruff check` clean. MrMultiVI was wired then **reverted** per user scope, verified back to its exact pre-session diff. **CONSEQUENCE (corrected 2026-08-06): the green CPU preflight *result* from job 25374994 is stale and must be re-run; the sealed snapshot is NOT stale and the contract may well not need re-rendering.** The earlier wording ("the contract must be re-rendered") overstated it. Evidence: `diff -rq` of `source-snapshots/f1b51341…` against the working tree shows exactly 3 differing files — the 3 edited here — with `benchmarks/` byte-identical; and the launcher runs the grid from inside the frozen snapshot (`cd $SNAPSHOT_ROOT`, `PYTHONPATH=$SNAPSHOT_ROOT/src`, `GIT_WORK_TREE=$SNAPSHOT_ROOT`), so the live tree is never imported. What moved is only the live-tree `git status` digest (`complete_dirty_status_sha256`, was `fe4dcf97…`). VERIFIED by reading `_validate_runtime` (`pre_submit.py:2017-2053`): the sole live-repo binding is `repository["head"] != contract["repository"]["expected_head"]` at `:2033` — and HEAD is still `cb343ca1` because nothing was committed. `complete_dirty_status_sha256` is only format-checked as a well-formed sha256 (`:2035-2039`), never compared to a pinned value; and the live `source_config_manifest` is compared only against *itself* and against the contract's schema **string** (`:2040-2053`) — never against the snapshot's pinned `source_config_manifest_digest` (`ca3e7546…`). **So the read-only preflight should still return `ready: true`; only the reported digest value differs.** Where the dirty-status digest genuinely binds is later: the authorization payload is built from a passing read-only result (`:2276`) and the job re-checks pre/postclaim at start. OPERATIONAL RULE: freeze the working tree from the moment the authorization is generated until the job starts | mrtotalvi | high | done | infrastructure | 2026-08-06 | mdmanurung | — |
| ANNBATCH: implement a MrTotalVI-specific AnnBatch datamodule with protein streaming, sample registry, save/load reconstruction, and parity tests. Upstream's generic AnnBatch datamodule emits RNA `X`, batch/label/sample, size factor and covariates but no protein-expression tensor or registry. Keep the validated in-memory AnnData loader for RDX-03 | mrtotalvi | medium | open | infrastructure | 2026-08-06 | mdmanurung | — |
| FINDINGS-STALE: FIXED 2026-08-06 — `.scratch/mrtotalvi-z-vs-totalvi/FINDINGS.md` carried unmarked n=1 stock-TOTALVI values that the pass-3 closure log wrongly claimed were fixed. Five numbers changed, all in the within-sample block: stock 15.62 ± 0.66 → 15.49 ± 0.51, z −38.9% → −38.3%, u −41.7% → −41.2%, quoted pooled gap −36.9% → −36.1%, and the RDX-03 reconciliation table's "here" value 17.28 → 17.08. Recomputed as `(value − base)/base`, verified by reproducing the old figures from the old base. No MrTotalVI value changed and no conclusion is affected. Both edits carry a visible "Corrected 2026-08-06" audit block listing every changed figure. The n=1 table under "Effective rank (earlier pass, n=1 baseline — retained for provenance)" was deliberately left alone. **PLEASE SPOT-CHECK the recomputed percentages** — this is an analysis artifact and the arithmetic is mine, not the original analyst's, so this stays open until a human confirms the five figures | mrtotalvi | medium | ready-for-human | analysis | 2026-08-06 | mdmanurung | — |
| ORPHAN-0803: INVESTIGATED 2026-08-06. `.scratch/mrtotalvi-v2-redesign/convergence-runs/20260726T161404Z-d5e85d96-c5074a79-b51065c8-failed/` reached **45 of 48 fits** — not one, as first recorded. Complete result/representation/worker/checkpoint quadruples for all 3 seeds of mixed, unequal_cells and sealed_500 (B1/B2/B3/D0) plus canonical_human B1/B2/B3; the only missing cell is `canonical_human_if_available--D0` × seeds 0/1/2. Its D0/seed0 checkpoint had already trained to `best-epoch=0399`; the worker then died in `run_convergence_fit.py` → `_verify_live_sources_against_manifest` with `ValueError: Live worker source differs from snapshot for benchmarks/mrtotalvi/__init__.py` — the Jul 30–31 latent-integrity work modified that file after the run's 2026-07-26 snapshot. Config/env/code manifests are dated 2026-07-26; results and `failure.json` 2026-08-03, so a 07-26 run was resumed off cached checkpoints and published as failed on 08-03. `status=failed`, `evidence_tier=pilot_cache`, unsealed, pre-ADR-0009 policy → **not promotable regardless**, and governance forbids splicing it. No matching SLURM job on 08-03 (`sacct` shows only unrelated `s2_exv_*` schisto jobs); the sole allocation spanning that moment is `mrvi_cp` 25349712 (07-31 → 08-04, TIMEOUT, highmem). REMAINING: confirm who launched it and on what | mrtotalvi | low | open | infrastructure | 2026-08-06 | mdmanurung | — |
| DOC-CONFLICTS: four tracker contradictions, recorded rather than silently patched — (1) D-041 `CONFIRMED` in `.living/decisions.md` vs `REFUTED FOR PROMOTION` in `benchmarks/ANALYSIS_MANIFEST.md`, same date; (2) `.claude/last-session.md`'s "RDX-03 rank gate may be unsound" is superseded by ADR-0009 (2026-07-31); (3) both `docs/plans/2026-07-2*.md` headers read `Status: not started` while `docs/review-clear-execute/*/tasks.md` is the operative checklist; (4) `.living/INDEX.md` (2026-07-08), `LOG_REGISTRY.md` (session 23), `FINDINGS_REGISTRY.md` (2026-07-13) predate ~30 sessions; (5) ADDED 2026-08-06 — **`CLAUDE.md` is the worst of these and was missed**, because it is auto-loaded into every session's context and so mis-orients each new session *before* any tracker is read. It states `**Active branch**: feat/cytoanvi` (git says `main`), frames the whole repo as a CytoANVI fork with no mention of MrTotalVI/MrMultiVI, gives a "Status" line about B1–B9 and a pending Leiden job as if that were current work, and its key-files table points at `src/cytoanvi/`, `benchmarks/cytoanvi/`, `tests/cytoanvi/` only. CytoANVI was shelved 2026-07-12. Rewrite the overview, status, key-files and active-conventions sections around the MrTotalVI track. Fix each when that tracker is next touched | infra | medium | open | documentation | 2026-08-06 | mdmanurung | — |
| P2-008: MoG-vs-VampPrior default rests on an UNPRESERVED artifact. The session-67 T/NK cluster-dispersion comparison (57k cells, 21 L3 labels, 6 clusters +32–90% radius) that set `u_prior="mog"` has no surviving file anywhere in scvi-tools/ or schisto_citeseq/analysis/ — same failure mode the D-041 adversarial audit caught. Additionally it measured UMAP/centroid geometry, which does not govern Leiden's kNN graph: on graph metrics VampPrior u beats MoG u on donor mixing (knnP 0.225 vs 0.273) and seed stability (Jaccard 0.189 vs 0.145) at equal cell-type purity. Re-run on the original T/NK data, recording whether it used u or z, before trusting the default for u-space clustering. STATUS CORRECTED 2026-08-06: was `open`, but the row's own text says the artifact is unrecoverable and the brief files it under *Blocked on data or compute* — `open` polluted the `grep`-for-actionable-work query the registry exists to serve. REVIVE WHEN: the original T/NK input data is located (schisto_citeseq/analysis/ or scvi-tools/ — neither currently holds it) | mrtotalvi | high | blocked | analysis | 2026-07-27 | mdmanurung | — |
| P2-007: VampPrior DA recipe + MoG-for-clustering rationale added to mr_multimodal.md (session-67 next-priority #2) | mrtotalvi | low | done | documentation | 2026-07-27 | mdmanurung | — |
| P2-006: `freeze_prior_after_init` docstring in `mrtotalvi/_model.py:102-105` + `mrmultivi/_model.py` still says "unvalidated candidate; no differential-abundance stability improvement is established" — contradicts D-041 (CONFIRMED, std=0.192). BLOCKER REWRITTEN 2026-08-06: the original condition ("until the RDX-03 48-fit run seals: every `src/scvi/**/*.py` byte-verified") is void because that run died at 42/48 on 2026-07-28. Now blocked on RDX-03-11 | mrtotalvi | medium | blocked | documentation | 2026-07-27 | mdmanurung | — |
| P2-005: Multi-dataset validation (macaque CITE-seq replication) — COMPUTE/DATA-BLOCKED: requires macaque dataset + GPU training. Macaque panel and homolog map were never frozen | mrtotalvi | medium | blocked | analysis | 2026-07-13 | mdmanurung | — |
| P2-004: differential_accessibility stub: add explicit mention in user guide that the method raises NotImplementedError | mrtotalvi | low | done | documentation | 2026-07-13 | mdmanurung | — |
| P2-003: No-intercept design matrix: note in docs that eps pre-centering absorbs intercept (V1-006) | mrtotalvi | low | done | documentation | 2026-07-13 | mdmanurung | — |
| P2-002: Document Chi2 df = n_per_cell (admissible samples, not n_latent) in analysis notes | mrtotalvi | low | done | documentation | 2026-07-13 | mdmanurung | — |
| P2-001: Add per-seed DA breakdown (s0=+2.74 s1=−9.04 s2=+9.67 / s0=0.84 s1=0.94 s2=1.09) to mr_multimodal.md | mrtotalvi | low | done | documentation | 2026-07-13 | mdmanurung | — |
| P1-006: MrTotalVI DA instability root-cause investigation (why std=9.46 across 3 seeds vs MrMultiVI std=0.126) — COMPUTE/DATA-BLOCKED: requires GPU + real schistosomiasis data. NARROWED session 68: representation instability is a contributor, not the mechanism (18–30% neighbourhood-stability gain vs 78% DA-variance reduction) | mrtotalvi | high | blocked | analysis | 2026-07-13 | mdmanurung | — |
| P1-005: MrMultiVI load() error message for AnnData misleads (says "provide n_genes/n_regions" instead of "use MuData") | mrtotalvi | low | done | infrastructure | 2026-07-13 | mdmanurung | — |
| P1-004: Backward-compat guard test: new flags (protein_in_encoder, n_latent_u defaults) at defaults → latents match pre-change baseline | mrtotalvi | medium | done | testing | 2026-07-13 | mdmanurung | — |
| P1-003: Statistical tests lack fixed seeds → flakiness risk (test_donor_axis_separation, test_mrtotalvi_lfc_sign_known_positive_control, etc.) | mrtotalvi | medium | done | testing | 2026-07-13 | mdmanurung | — |
| P1-002: n_labels==0 / unlabeled_category path untested — guard all classifier forward passes in MrTotalVI/MrMultiVI | mrtotalvi | medium | done | testing | 2026-07-13 | mdmanurung | — |
| P1-001: DA multi-seed calibration test in CI (currently only in .scratch; no seed-stability assertion for DA) | mrtotalvi | high | done | testing | 2026-07-13 | mdmanurung | — |
| LFC B5: update ADR-0005/0006 + mr_multimodal.md to remove "not implemented" + record D-020–D-023 | mrtotalvi | medium | done | documentation | 2026-07-11 | mdmanurung | — |
| LFC B4: add store_lfc tests (shape/coord/finiteness, pde∈[0,1], D2 determinism, backward-compat) | mrtotalvi | high | done | testing | 2026-07-11 | mdmanurung | — |
| LFC B3: add LFC kwargs + xarray assembly to `mrtotalvi/_model.py` and `mrmultivi/_model.py` wrappers | mrtotalvi | high | done | infrastructure | 2026-07-11 | mdmanurung | — |
| LFC B2: remove NotImplementedError from `_stats.py`, extend `_construct_design_matrix`, port MRVI LFC block | mrtotalvi | high | done | infrastructure | 2026-07-11 | mdmanurung | — |
| LFC B1: implement `compute_h_from_x_eps` in MrTotalVAE + MrMultiVAE modules | mrtotalvi | high | done | infrastructure | 2026-07-11 | mdmanurung | — |
| CYTO-NITS: six residual code-quality/perf items in `.scratch/cytoanvi-review-fixes.md` — vectorize the O(n³) transitivity check in `validate_reachability_matrix`, vectorize one-time init loops, split `run_tree_arches_pipeline` (14 params, `hierarchy.py:288-390`), shorten `__init__`/`prepare_query_anndata`, `key in d` idiom nits, hoist `batch_size=256` to a constant. All P0/P1 correctness findings from that review pass are closed; `cytoanvi-review-patch-plan.md` is COMPLETE and `cytoanvi-scientific-review-fixes.md` has one stale `[~]` (P4-A) whose run in fact finished. REVIVE WHEN: CytoANVI is unshelved | cytoanvi | low | shelved | infrastructure | 2026-08-06 | mdmanurung | — |
| Full Nuñez/Roider benchmarks at max_epochs=1000 — B1/B2/B3/B5/B8 complete at 3 seeds; B4/B6/B9 remain gated by the individual rows below. REVIVE WHEN: CytoANVI is unshelved and those gates clear | cytoanvi | critical | shelved | analysis | 2026-06-10 | mdmanurung | [b-track-full-benchmarks.md](b-track-full-benchmarks.md) |
| Real case/control axis for B4 biological validation — B4 is PLUMBING ONLY (pseudo-batch split). REVIVE WHEN: a real case/control cytometry cohort exists | cytoanvi | high | shelved | analysis | 2026-06-18 | mdmanurung | — |
| Tune and document CytoVI-specific default λ after B6 sweep — same root cause as B4, B6 is plumbing-only. REVIVE WHEN: real continual-update data exists | cytoanvi | medium | shelved | analysis | 2026-06-18 | mdmanurung | — |
| Push feat/cytoanvi branch + upstream PR | cytoanvi | high | done | infrastructure | 2026-06-18 | mdmanurung | — |
| B9 mapQC on query controls after surgery — superseded by the 2026-07-06 row below. REVIVE WHEN: `mapqc` is patched | cytoanvi | medium | shelved | validation | 2026-06-29 | mdmanurung | — |
| Roider Phase-3 B3 full-cohort (job 25140597) — CLOSED 2026-08-06: superseded by jobs 25145151/52/53 (see "B3 RERUN COMPLETE") and finalised in F-012. Row was left `in_progress` for a month | cytoanvi | critical | done | analysis | 2026-07-03 | mdmanurung | — |
| Roider Phase-3 B5 holdout sweep (job 25140598) — CLOSED 2026-08-06: superseded by jobs 25149032/33/34 and finalised in F-013. Row was left `in_progress` for a month | cytoanvi | critical | done | analysis | 2026-07-03 | mdmanurung | — |
| Nuñez B8 seed-2 recovery (job 25108052 running ~3.5h as of 2026-06-29) | cytoanvi | high | done | analysis | 2026-06-29 | mdmanurung | — |
| Run manifest-mode aggregation after all Phase-3/5 artifacts land | cytoanvi | high | done | infrastructure | 2026-06-29 | mdmanurung | — |
| B5 REDESIGN COMPLETE: jobs 25145052/53/54 (seeds 0/1/2, 11 types, CytoVI baseline, batch 16384) + merge 25145055. RESULT: CytoANVI mean_auroc 0.484±0.019 (NEGATIVE) vs CytoVI kNN 0.775±0.002. Diagnostic jobs 25149032/33/34 running (CytoANVI-kNN in own latent) | cytoanvi | high | done | analysis | 2026-07-04 | mdmanurung | — |
| Fix ruff lint findings on cytoanvi package | cytoanvi | medium | done | infrastructure | 2026-06-29 | mdmanurung | — |
| Fix B5 AUROC SE formula (Wilcoxon numerator missing — FDR all-significant) | cytoanvi | critical | done | analysis | 2026-06-30 | mdmanurung | — |
| Fix unseeded np.random.choice in test_hierarchy_schpl_mock.py | cytoanvi | low | done | testing | 2026-06-30 | mdmanurung | — |
| Fix NAN_LAYER local redefinition in tasks_imputation.py (DRY violation) | cytoanvi | low | done | infrastructure | 2026-06-30 | mdmanurung | — |
| Regenerate B5 result JSONs after AUROC SE fix (existing values are wrong) | cytoanvi | critical | done | analysis | 2026-06-30 | mdmanurung | — |
| Install `mapqc` in conda env before next SLURM submission (unblocks B9) | cytoanvi | high | done | infrastructure | 2026-07-03 | mdmanurung | — |
| USER ACTION: scancel stale jobs 25102610 25102547 (25102546 already gone) | cytoanvi | high | done | infrastructure | 2026-07-03 | mdmanurung | — |
| Monitor B1 Nuñez reversal on full-cohort run (F-003: resolved — inductive Δ+0.017) | cytoanvi | high | done | validation | 2026-06-29 | mdmanurung | — |
| Re-run Nuñez B1 e1000 seeds 0/1/2 after inductive kNN annotation fix (PID 3186154 v3) | cytoanvi | critical | done | analysis | 2026-06-30 | mdmanurung | — |
| Fix Nuñez transductive leakage in annotate_nunez.py (L-022) | cytoanvi | critical | done | analysis | 2026-06-30 | mdmanurung | — |
| Fix accelerator='gpu' → 'auto' in training.py (F14) | cytoanvi | medium | done | infrastructure | 2026-06-30 | mdmanurung | — |
| Add B9 round-robin sample comment + B5 calibration_note (F4/F6) | cytoanvi | low | done | analysis | 2026-06-30 | mdmanurung | — |
| B3 aggregator backward-compat for key rename (F2) | cytoanvi | medium | done | infrastructure | 2026-06-30 | mdmanurung | — |
| XGBoost, Phenograph, FlowSOM baselines for B1 (F15) | cytoanvi | high | done | analysis | 2026-06-30 | mdmanurung | — |
| Fix mask_augment nan_mask branch global RNG (F20) | cytoanvi | low | done | infrastructure | 2026-06-30 | mdmanurung | — |
| Correct ARCSINH_COFACTORS dict (F13) | cytoanvi | low | done | documentation | 2026-06-30 | mdmanurung | — |
| Consolidate MockTreeNode via conftest import (F30) | cytoanvi | low | done | testing | 2026-06-30 | mdmanurung | — |
| Publication-readiness review + fixes (6 parallel agents) — code/docs/tests/packaging/benchmark-reporting | cytoanvi | critical | done | infrastructure | 2026-07-03 | mdmanurung | — |
| Fix AnnData.concatenate → anndata.concat in hierarchy.py update path (L-032) | cytoanvi | high | done | infrastructure | 2026-07-03 | mdmanurung | — |
| Document EWC Fisher = (E[grad])² approximation; λ not portable (L-033) | cytoanvi | medium | done | documentation | 2026-07-03 | mdmanurung | — |
| Add direct ELBO-component unit tests (L-034); Fisher sanity; training-descends; slow-marker CI guard | cytoanvi | high | done | testing | 2026-07-03 | mdmanurung | — |
| Standalone packaging: name=cytoanvi, dual-BSD LICENSE, sdist excludes, .gitattributes (D-008) | cytoanvi | high | done | infrastructure | 2026-07-03 | mdmanurung | — |
| B5 mean_auroc primary; B3 concordance-not-accuracy relabel; B4/B6/B9/B8 demoted in manifests (D-009/D-010) | cytoanvi | high | done | analysis | 2026-07-03 | mdmanurung | — |
| BLOCKER (needs data): independent manually-gated panel-2 labels for a real B3 accuracy claim — current B3 "concordance" (F-012, 0.671±0.008) is model-vs-model agreement, not ground-truth accuracy. REVIVE WHEN: independent expert-gated Panel-2 labels exist | cytoanvi | critical | shelved | analysis | 2026-07-03 | mdmanurung | — |
| BLOCKER (needs compute): rerun B5 roider-full at 3 seeds (currently seeds=[0] only) | cytoanvi | critical | done | analysis | 2026-07-03 | mdmanurung | — |
| BLOCKER (needs data): external novel-cell-type dataset to make B5 mean AUROC meaningful (>0.7) — currently 0.484±0.005, below chance (F-013/D-040). REVIVE WHEN: an external novel-cell-type dataset is available | cytoanvi | high | shelved | analysis | 2026-07-03 | mdmanurung | — |
| Fill real CytoANVI/CytoVI preprint DOIs in docs once posted (currently "in preparation"). REVIVE WHEN: the preprints are posted | cytoanvi | medium | shelved | documentation | 2026-07-03 | mdmanurung | — |
| Deferred code items DONE: EWC per-sample Fisher (exact E[grad²], default); GPU-sync opt-out (CYTOANVI_DISABLE_FINITE_CHECKS); reconst_loss→reconstruction_term rename | cytoanvi | low | done | infrastructure | 2026-07-03 | mdmanurung | — |
| Session-13 actions DONE: scancel 3 stale jobs; mapqc installed (B9 import unblocked); B5 seeds 1-2 submitted (25144240/1) | cytoanvi | high | done | infrastructure | 2026-07-03 | mdmanurung | — |
| B9 mapQC FAILED AGAIN 2026-07-06: job 25149329 ExitCode 1:0. mapqc 0.1.1 installed but IndexError in `_get_per_cell_filtering_info` (`mode().iloc[0]` on empty result). Library bug. Roider B9 additionally blocked by `ValueError: Category 31 not found in source registry` (missing `extend_categories=True`), see F-039. REVIVE WHEN: `mapqc` patches the IndexError, or a dataset workaround is found | cytoanvi | medium | shelved | validation | 2026-07-06 | mdmanurung | — |
| B1 Roider full-cohort COMPLETE 2026-07-06: jobs 25149326/27/28. CytoANVI 0.9317±0.0022 vs kNN 0.8928±0.0034, Δ+0.0388±0.0018 ✅ gate. XGBoost 0.9516. Aggregated → roider_full_b1_multiseed.json. publication_manifest.json updated to complete | cytoanvi | high | done | analysis | 2026-07-06 | mdmanurung | — |
| Engineering maturity DONE: fixed 15 CI workflows (scvi-tools→cytoanvi install target), py.typed, __version__, ruff lint+format clean, build verified (L-036) | cytoanvi | high | done | infrastructure | 2026-07-03 | mdmanurung | — |
| scvi/ namespace collision (L-035): DEFERRED per D-011 — internal/clean-env use only, not on PyPI. REVIVE WHEN: a public PyPI upload is planned; decide Replace vs Coexist vs upstream-PR first | cytoanvi | high | shelved | infrastructure | 2026-07-03 | mdmanurung | — |
| Reset dist version from inherited scvi-tools 1.5.0rc1 for standalone cytoanvi release (release-strategy) | cytoanvi | medium | done | infrastructure | 2026-07-03 | mdmanurung | — |
| Leiden recompute FIXED (L-040): scanpy 1.12/igraph 1.0.0 seed incompat — unblocks coarser resolutions (faster training + more interpretable labels) | cytoanvi | medium | done | infrastructure | 2026-07-04 | mdmanurung | — |
| B5 DIAGNOSTIC: kNN-distance OOD in CytoANVI's OWN latent (jobs 25149032/33/34) — CLOSED 2026-08-06: completed 2026-07-12, CytoANVI-latent kNN-OOD 0.906±0.003, resolving the TTA-vs-latent question (F-013/D-040: the TTA method is the failure, not the latent). Row was left `in_progress` | cytoanvi | high | done | analysis | 2026-07-05 | mdmanurung | — |
| COARSE-Leiden B3/B5 PREPARED (L-041 speed lever): 6 scripts phase3{a,b}_{b3,b5}coarse_*.slurm w/ __RES__ placeholder; calibration job 25145242 picks resolution ~12 clusters. Never submitted; calibration job 25211796 still pending since session 67. REVIVE WHEN: CytoANVI is unshelved | cytoanvi | medium | shelved | analysis | 2026-07-04 | mdmanurung | — |
| B3 RERUN COMPLETE: jobs 25145151/52/53 (seeds 0/1/2). RESULT: p1 macro-F1 0.828±0.015 ✅, p2 concordance 0.671±0.008 ❌ (gate ≥0.80 NOT met — concordance not accuracy). Panel-2 ground-truth labels still unavailable (see BLOCKER row) | cytoanvi | high | done | analysis | 2026-07-04 | mdmanurung | — |
